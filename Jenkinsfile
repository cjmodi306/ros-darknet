pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
  		sh 'Starting...'
	     }
	}
	stage('Test') {
            steps {
               dir('.'){sh 'ls'}
		  }
        }
     }
}
