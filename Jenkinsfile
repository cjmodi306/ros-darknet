pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Starting build..."'
	     }
	}
	stage('Test') {
            steps {
                sh 'echo "Starting test..."'
		  }
        }
     }
}
